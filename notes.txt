# make sure you’re in your labs repo and schemas311 env
cd ~/src/synesthetic-labs
conda activate schemas311

export MCP_ADAPTER_CMD="docker exec -i synesthetic-mcp-serve-1 python -m mcp"
export LABS_FAIL_FAST=1


cd ~/src/synesthetic-labs
python -m labs.cli generate "make a circle harmony asset"


# now generate a test asset end-to-end
python -m labs.cli generate "make a circle harmony asset"


export MCP_ADAPTER_CMD="docker compose -p synesthetic-mcp -f /home/dce/src/synesthetic-mcp/docker-compose.yml run --rm -T serve"
export LABS_FAIL_FAST=1



python -m labs.cli generate "make a circle harmony asset"




export MCP_ADAPTER_CMD="docker exec -i synesthetic-mcp-serve-1 python -m mcp"
export LABS_FAIL_FAST=1
























python - <<'PY'
import os
from dotenv import load_dotenv
load_dotenv(dotenv_path=".env", override=True)
for k in ["LABS_EXTERNAL_LIVE","GEMINI_API_KEY","GEMINI_ENDPOINT","LABS_SCHEMA_VERSION"]:
    print(f"{k}={os.getenv(k)}")
PY

