# TCP is the default when MCP_ENDPOINT is unset
MCP_HOST=127.0.0.1
MCP_PORT=8765

# Uncomment to use the bundled STDIO adapter
# MCP_ENDPOINT=stdio
# MCP_ADAPTER_CMD=python -m labs.mcp_stub

# Uncomment to use the optional Unix socket transport
# MCP_ENDPOINT=socket
# MCP_SOCKET_PATH=/tmp/synesthetic.sock

# Deprecated: only used for STDIO adapter; ignored for TCP/socket
# SYN_SCHEMAS_DIR=/abs/path/to/schemas
LABS_EXPERIMENTS_DIR=meta/output/labs/experiments
LABS_FAIL_FAST=1
