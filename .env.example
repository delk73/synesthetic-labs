# Synesthetic Labs Environment Variables

# External Generation - set to 1 to enable live mode (otherwise mock responses are used)
LABS_EXTERNAL_LIVE=1

# Gemini API settings (required for live generation with gemini)
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.0-flash

# OpenAI API settings (required for live generation with openai)
OPENAI_API_KEY=your_openai_api_key_here

# MCP settings
LABS_MCP_SOCKET=vscode://path/to/socket-file  # Standard VSCode socket path
LABS_MCP_TCP=localhost:9000  # TCP fallback if socket is unavailable

# Validation behavior
LABS_FAIL_FAST=0  # Set to 1 for strict mode, 0 for relaxed validation

# Schema version (0.7.3 or 0.7.4)
LABS_SCHEMA_VERSION=0.7.3

# Logging level
LABS_LOG_LEVEL=INFO  # Set to DEBUG for more verbose output