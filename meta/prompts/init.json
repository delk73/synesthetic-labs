{
  "task": "Emit synesthetic-labs repo (v0.1 Generator + Critic)",
  "objective": "Ship a runnable generator → critic loop with logging, CLI, tests, docs, and container harness.",
  "constraints": {
    "language": "Python >=3.11",
    "style": "KISS, deterministic, minimal deps",
    "dependencies": ["argparse", "json", "logging", "datetime", "uuid", "pytest"],
    "runtime": "pytest -q and Docker harness",
    "logging": "All agent actions log to JSONL sinks under meta/output/",
    "documentation": "README references docs/labs_spec.md"
  },
  "file_outlines": {
    "labs/agents/generator.py": "GeneratorAgent.propose(prompt) -> {id,timestamp,prompt,provenance}",
    "labs/agents/critic.py": "CriticAgent.review(asset) -> asset + ok/issues",
    "labs/logging.py": "log_jsonl(path, record) helper",
    "labs/cli.py": "argparse CLI with generate/critique subcommands",
    "tests/": "pytest suite covering generator, critic, pipeline",
    "Dockerfile": "python:3.11-slim running pytest -q",
    "docker-compose.yml": "labs service invoking pytest -q",
    "test.sh": "build image then run docker compose",
    "docs/labs_spec.md": "Specification for v0.1 loop"
  },
  "exit_criteria": [
    "GeneratorAgent and CriticAgent implemented",
    "CLI prints JSON for generate and critique subcommands",
    "pytest -q passes",
    "Docker harness builds and runs",
    "Documentation and metadata updated"
  ]
}
